{
  "name": "sovereign-second-brain-plugin",
  "version": "0.1.0",
  "description": "Local, offline-capable intelligence layer for Obsidian (plugin side)",
  "main": "main.js",
  "private": true,
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "npm run typecheck && node esbuild.config.mjs production",
    "typecheck": "tsc -noEmit -skipLibCheck",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "esbuild": "^0.24.0",
    "obsidian": "^1.7.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
